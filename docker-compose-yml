version: '3.9'

services:
  api_chat:
    build: .
    container_name: api_chat
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/kavak
    ports:
      - "8000:8000"
    env_file:
      - .env
